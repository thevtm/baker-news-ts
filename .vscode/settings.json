{
  "deno.enable": true,

  // Only backend is using Deno
  "deno.config": "./backend/deno.json",
  "deno.enablePaths": ["./backend"],

  // This is required for the tests to run with the correct environment variables
  "deno.testing.args": ["--allow-all", "--env-file=./backend/.env.test"]
}
